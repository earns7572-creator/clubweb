/** Club Craft PA Miniatures — dark physical cabinets, internal emitters and short local optical halos. */
import * as THREE from "three";
import { useEffect, useMemo } from "react";
import { useThree } from "@react-three/fiber";
import type { SpeakerKind } from "@/hooks/useClubAudio";

type Props = { kind: SpeakerKind; activity: number; selected?: boolean; showHaze?: boolean; idleVisible?: boolean };
type Blueprint = { body: [number, number, number]; cabinet: THREE.ExtrudeGeometry; edges: THREE.EdgesGeometry };
type SpeakerMaterials = { cabinet: THREE.MeshStandardMaterial; baffle: THREE.MeshStandardMaterial; horn: THREE.MeshStandardMaterial; driver: THREE.MeshStandardMaterial };
type HornProps = { geometry: THREE.BufferGeometry; x?: number; y: number; frontZ: number; mouthWidth: number; mouthHeight: number; throatWidth: number; throatHeight: number; depth: number; materials: SpeakerMaterials };

const passiveFrameMaterial = new THREE.MeshStandardMaterial({ color: "#353632", roughness: .74, metalness: .12 });
const passiveDetailMaterial = new THREE.MeshStandardMaterial({ color: "#242522", roughness: .86, metalness: .03 });
const passiveMetalMaterial = new THREE.MeshStandardMaterial({ color: "#696a64", roughness: .5, metalness: .24 });
const passiveFootMaterial = new THREE.MeshStandardMaterial({ color: "#1c1d1b", roughness: .9 });
const selectionMaterial = new THREE.MeshBasicMaterial({ color: "#d5c2a8", transparent: true, opacity: .028, side: THREE.BackSide });
const idleEdgeMaterial = new THREE.LineBasicMaterial({ color: "#777872", transparent: true, opacity: .38 });
const unitBox = new THREE.BoxGeometry(1, 1, 1);
const wooferMount = new THREE.CylinderGeometry(1, 1, .028, 24);
const wooferSurround = new THREE.TorusGeometry(1, .13, 6, 24);
const wooferCone = new THREE.CircleGeometry(1, 24);
const emitterDisc = new THREE.CircleGeometry(1, 20);
const dustCap = new THREE.SphereGeometry(1, 16, 10);
const fastenerHead = new THREE.CylinderGeometry(.038, .038, .018, 12);

function makeChamferCabinet(width: number, height: number, depth: number, taper = 0) { const top = width * (1 - taper); const shape = new THREE.Shape(); shape.moveTo(-width / 2, -height / 2); shape.lineTo(width / 2, -height / 2); shape.lineTo(top / 2, height / 2); shape.lineTo(-top / 2, height / 2); shape.closePath(); const geometry = new THREE.ExtrudeGeometry(shape, { depth, bevelEnabled: true, bevelThickness: .036, bevelSize: .028, bevelSegments: 1, curveSegments: 1 }); geometry.translate(0, 0, -depth / 2); geometry.computeVertexNormals(); return geometry; }
function makeHornFlare(mouthWidth: number, mouthHeight: number, throatWidth: number, throatHeight: number, depth: number) { const vertices = new Float32Array([-mouthWidth / 2, -mouthHeight / 2, 0, mouthWidth / 2, -mouthHeight / 2, 0, mouthWidth / 2, mouthHeight / 2, 0, -mouthWidth / 2, mouthHeight / 2, 0, -throatWidth / 2, -throatHeight / 2, -depth, throatWidth / 2, -throatHeight / 2, -depth, throatWidth / 2, throatHeight / 2, -depth, -throatWidth / 2, throatHeight / 2, -depth]); const index = [0, 1, 5, 0, 5, 4, 1, 2, 6, 1, 6, 5, 2, 3, 7, 2, 7, 6, 3, 0, 4, 3, 4, 7]; const geometry = new THREE.BufferGeometry(); geometry.setAttribute("position", new THREE.BufferAttribute(vertices, 3)); geometry.setIndex(index); geometry.computeVertexNormals(); return geometry; }
const foldedSubHorn = makeHornFlare(.86, .58, .22, .16, .3);
const upperWooferHorn = makeHornFlare(.78, .32, .18, .09, .28);
const lowerWooferHorn = makeHornFlare(.78, .44, .2, .13, .3);
const fullHorn = makeHornFlare(.76, .36, .16, .09, .31);
const midHorn = makeHornFlare(.56, .2, .12, .07, .19);
const highHorn = makeHornFlare(.88, .38, .12, .08, .36);
function makeBlueprint(body: [number, number, number], taper: number): Blueprint { const cabinet = makeChamferCabinet(...body, taper); return { body, cabinet, edges: new THREE.EdgesGeometry(cabinet, 14) }; }
const blueprints: Record<SpeakerKind, Blueprint> = { sub: makeBlueprint([2.36, .9, 1.4], .015), woofer: makeBlueprint([1.15, 1.5, 1.08], .04), full: makeBlueprint([1.1, 2, 1.08], .1), mid: makeBlueprint([.84, .88, .58], .035), high: makeBlueprint([1.08, .62, .72], .08) };

const LOW = new THREE.Color("#ff3b30"); const MID = new THREE.Color("#ffd60a"); const HIGH = new THREE.Color("#32d05b");
const neutralCabinetColor = new THREE.Color("#8f908b"); const neutralBaffleColor = new THREE.Color("#686964"); const neutralHornColor = new THREE.Color("#30312e"); const neutralDriverColor = new THREE.Color("#272824");
const speakerBandProfile: Record<SpeakerKind, { horn: number; driver: number }> = { sub: { horn: 0, driver: 0 }, woofer: { horn: .12, driver: .04 }, full: { horn: 1, driver: 0 }, mid: { horn: .52, driver: .5 }, high: { horn: 1, driver: 1 } };
const typeGlowGain: Record<SpeakerKind, { horn: number; driver: number }> = { sub: { horn: 1.1, driver: 0 }, woofer: { horn: .95, driver: .22 }, full: { horn: 1.5, driver: 1.05 }, mid: { horn: 1.05, driver: .92 }, high: { horn: 1.5, driver: 1.08 } };
function setFrequencyColor(target: THREE.Color, position: number) { const band = THREE.MathUtils.clamp(position, 0, 1); return band <= .5 ? target.copy(LOW).lerp(MID, band * 2) : target.copy(MID).lerp(HIGH, (band - .5) * 2); }
function createSpeakerMaterials(): SpeakerMaterials { return { cabinet: new THREE.MeshStandardMaterial({ color: neutralCabinetColor, roughness: .84, metalness: .02, emissive: "#000000" }), baffle: new THREE.MeshStandardMaterial({ color: neutralBaffleColor, roughness: .9, metalness: .01, emissive: "#000000" }), horn: new THREE.MeshStandardMaterial({ color: neutralHornColor, roughness: .79, metalness: .01, side: THREE.DoubleSide, emissive: "#000000" }), driver: new THREE.MeshStandardMaterial({ color: neutralDriverColor, roughness: .72, metalness: .01, side: THREE.DoubleSide, emissive: "#000000" }) }; }
function useSpeakerMaterials(kind: SpeakerKind, activity: number) { const materials = useMemo(createSpeakerMaterials, []); const { invalidate } = useThree(); useEffect(() => () => { Object.values(materials).forEach((material) => material.dispose()); }, [materials]); useEffect(() => { const active = Math.pow(THREE.MathUtils.clamp(activity, 0, 1), 1.15); const bands = speakerBandProfile[kind]; const gains = typeGlowGain[kind]; materials.cabinet.color.copy(neutralCabinetColor); materials.baffle.color.copy(neutralBaffleColor); materials.horn.color.copy(neutralHornColor); materials.driver.color.copy(neutralDriverColor); setFrequencyColor(materials.horn.emissive, bands.horn); setFrequencyColor(materials.driver.emissive, bands.driver); materials.cabinet.emissiveIntensity = 0; materials.baffle.emissiveIntensity = kind === "full" ? active * .045 : 0; materials.horn.emissiveIntensity = active * gains.horn; materials.driver.emissiveIntensity = active * gains.driver; invalidate(); }, [activity, invalidate, kind, materials]); return materials; }

function createGlowTexture(size = 64) { const data = new Uint8Array(size * size * 4); for (let y = 0; y < size; y += 1) for (let x = 0; x < size; x += 1) { const nx = x / (size - 1) * 2 - 1; const ny = y / (size - 1) * 2 - 1; const alpha = Math.pow(Math.max(0, 1 - Math.sqrt(nx * nx + ny * ny)), 2.5); const index = (y * size + x) * 4; data[index] = 255; data[index + 1] = 255; data[index + 2] = 255; data[index + 3] = Math.round(alpha * 255); } const texture = new THREE.DataTexture(data, size, size, THREE.RGBAFormat); texture.needsUpdate = true; return texture; }
const sharedGlowTexture = createGlowTexture();
function LocalGlow({ color, activity, position, size }: { color: THREE.Color; activity: number; position: [number, number, number]; size: number }) { const material = useMemo(() => new THREE.SpriteMaterial({ map: sharedGlowTexture, transparent: true, depthWrite: false, depthTest: false, blending: THREE.AdditiveBlending }), []); useEffect(() => () => material.dispose(), [material]); useEffect(() => { material.color.copy(color); material.opacity = Math.pow(THREE.MathUtils.clamp(activity, 0, 1), 1.35) * .28; }, [activity, color, material]); const scale = size * (1 + activity * .08); return <sprite material={material} position={position} scale={[scale, scale, 1]} renderOrder={4} />; }
function CabinetFeet({ width, height, depth, count = 2 }: { width: number; height: number; depth: number; count?: number }) { const positions = count === 4 ? [[-.37, -.3], [.37, -.3], [-.37, .3], [.37, .3]] : [[-.34, .12], [.34, .12]]; return <group position={[0, -height / 2 - .05, 0]}>{positions.map(([x, z], index) => <mesh key={index} geometry={unitBox} position={[x * width, 0, z * depth]} scale={[.1, .1, .12]} material={passiveFootMaterial} />)}</group>; }
function CornerFasteners({ width, height, frontZ, inset = .08 }: { width: number; height: number; frontZ: number; inset?: number }) { return <>{[[-1, -1], [1, -1], [-1, 1], [1, 1]].map(([x, y], index) => <mesh key={index} geometry={fastenerHead} rotation={[Math.PI / 2, 0, 0]} position={[x * (width / 2 - inset), y * (height / 2 - inset), frontZ]} material={passiveMetalMaterial} />)}</>; }
function WooferAssembly({ size, y, frontZ, materials }: { size: number; y: number; frontZ: number; materials: SpeakerMaterials }) { return <group position={[0, y, frontZ]}><mesh geometry={wooferMount} rotation={[Math.PI / 2, 0, 0]} scale={[size * 1.15, size * 1.15, 1]} material={materials.baffle} /><mesh geometry={wooferSurround} position={[0, 0, .03]} scale={[size, size, 1]} material={materials.baffle} /><mesh geometry={wooferCone} position={[0, 0, .045]} scale={[size * .8, size * .8, 1]} material={passiveDetailMaterial} /><mesh geometry={dustCap} position={[0, 0, .052]} scale={[size * .22, size * .22, size * .065]} material={passiveDetailMaterial} /><mesh geometry={emitterDisc} position={[0, 0, .058]} scale={[size * .28, size * .28, 1]} material={materials.driver} /></group>; }
function HornFlare({ geometry, x = 0, y, frontZ, mouthWidth, mouthHeight, throatWidth, throatHeight, depth, materials }: HornProps) { void geometry; return <group position={[x, y, frontZ]}><mesh geometry={unitBox} position={[0, 0, -.028]} scale={[mouthWidth * .94, mouthHeight * .94, .055]} material={passiveDetailMaterial} /><mesh geometry={unitBox} position={[0, mouthHeight / 2, .012]} scale={[mouthWidth, .03, .04]} material={passiveFrameMaterial} /><mesh geometry={unitBox} position={[0, -mouthHeight / 2, .012]} scale={[mouthWidth, .03, .04]} material={passiveFrameMaterial} /><mesh geometry={unitBox} position={[-mouthWidth / 2, 0, .012]} scale={[.03, mouthHeight, .04]} material={passiveFrameMaterial} /><mesh geometry={unitBox} position={[mouthWidth / 2, 0, .012]} scale={[.03, mouthHeight, .04]} material={passiveFrameMaterial} /><mesh geometry={unitBox} position={[0, 0, -.065]} scale={[throatWidth, throatHeight, Math.max(depth * .5, .05)]} material={materials.horn} /><mesh geometry={emitterDisc} position={[0, 0, .025]} scale={[Math.max(throatWidth * 1.35, .075), Math.max(throatHeight * 1.5, .06), 1]} material={materials.horn} /></group>; }
function ReflexPort({ x, y, frontZ, width, height }: { x: number; y: number; frontZ: number; width: number; height: number }) { return <mesh geometry={unitBox} position={[x, y, frontZ]} scale={[width, height, .045]} material={passiveDetailMaterial} />; }
function FoldedHorn({ geometry, x = 0, y, frontZ, mouthWidth, mouthHeight, throatWidth, throatHeight, depth, materials }: HornProps) { return <group position={[x, y, frontZ]}><HornFlare geometry={geometry} y={0} frontZ={0} mouthWidth={mouthWidth} mouthHeight={mouthHeight} throatWidth={throatWidth} throatHeight={throatHeight} depth={depth} materials={materials} /><mesh geometry={unitBox} position={[0, 0, -depth - .08]} scale={[throatWidth * .7, throatHeight * .7, .04]} material={materials.horn} /></group>; }
function BafflePlate({ width, height, frontZ, materials }: { width: number; height: number; frontZ: number; materials: SpeakerMaterials }) { return <mesh geometry={unitBox} position={[0, 0, frontZ]} scale={[width, height, .045]} material={materials.baffle} />; }

function SubModel({ blueprint, materials, activity, haze }: { blueprint: Blueprint; materials: SpeakerMaterials; activity: number; haze: boolean }) { const [width, height, depth] = blueprint.body; const frontZ = depth / 2 + .012; return <><mesh geometry={blueprint.cabinet} material={materials.cabinet} /><FoldedHorn geometry={foldedSubHorn} x={-.55} y={.01} frontZ={frontZ} mouthWidth={.86} mouthHeight={.58} throatWidth={.22} throatHeight={.16} depth={.3} materials={materials} /><FoldedHorn geometry={foldedSubHorn} x={.55} y={.01} frontZ={frontZ} mouthWidth={.86} mouthHeight={.58} throatWidth={.22} throatHeight={.16} depth={.3} materials={materials} /><mesh geometry={unitBox} position={[0, 0, frontZ + .014]} scale={[.055, height * .65, .05]} material={passiveFrameMaterial} /><CornerFasteners width={width * .88} height={height * .76} frontZ={frontZ + .03} inset={.06} /><CabinetFeet width={width} height={height} depth={depth} count={4} />{haze && <LocalGlow color={LOW} activity={activity} position={[0, .01, frontZ + .032]} size={.98} />}</>; }
function WooferModel({ blueprint, materials, activity, haze }: { blueprint: Blueprint; materials: SpeakerMaterials; activity: number; haze: boolean }) { const [width, height, depth] = blueprint.body; const frontZ = depth / 2 + .012; return <><mesh geometry={blueprint.cabinet} material={materials.cabinet} /><FoldedHorn geometry={upperWooferHorn} y={.38} frontZ={frontZ} mouthWidth={.78} mouthHeight={.32} throatWidth={.18} throatHeight={.09} depth={.28} materials={materials} /><FoldedHorn geometry={lowerWooferHorn} y={-.36} frontZ={frontZ} mouthWidth={.78} mouthHeight={.44} throatWidth={.2} throatHeight={.13} depth={.3} materials={materials} /><mesh geometry={unitBox} position={[0, .02, frontZ + .014]} scale={[width * .72, .05, .05]} material={passiveDetailMaterial} /><CornerFasteners width={width * .86} height={height * .85} frontZ={frontZ + .03} /><CabinetFeet width={width} height={height} depth={depth} />{haze && <LocalGlow color={LOW} activity={activity} position={[0, -.35, frontZ + .03]} size={.64} />}</>; }
function FullModel({ blueprint, materials, activity, haze }: { blueprint: Blueprint; materials: SpeakerMaterials; activity: number; haze: boolean }) { const [width, height, depth] = blueprint.body; const frontZ = depth / 2 + .012; return <group rotation={[-.04, 0, 0]}><mesh geometry={unitBox} scale={[width, height, depth]} material={materials.cabinet} /><mesh geometry={unitBox} position={[0, .56, frontZ + .018]} scale={[.78, .38, .055]} material={passiveDetailMaterial} /><mesh geometry={emitterDisc} position={[0, .56, frontZ + .05]} scale={[.085, .055, 1]} material={materials.horn} /><mesh geometry={wooferSurround} position={[0, -.08, frontZ + .035]} scale={[.26, .26, 1]} material={materials.baffle} /><mesh geometry={emitterDisc} position={[0, -.08, frontZ + .042]} scale={[.2, .2, 1]} material={passiveDetailMaterial} /><mesh geometry={emitterDisc} position={[0, -.08, frontZ + .047]} scale={[.06, .06, 1]} material={materials.driver} /><ReflexPort x={-.24} y={-.72} frontZ={frontZ + .03} width={.17} height={.055} /><ReflexPort x={.24} y={-.72} frontZ={frontZ + .03} width={.17} height={.055} /><CornerFasteners width={width * .84} height={height * .85} frontZ={frontZ + .035} /><CabinetFeet width={width} height={height} depth={depth} />{haze && <><LocalGlow color={LOW} activity={activity} position={[0, -.08, frontZ + .04]} size={.38} /><LocalGlow color={HIGH} activity={activity} position={[0, .56, frontZ + .035]} size={.4} /></>}</group>; }
function MidModel({ blueprint, materials, activity, haze }: { blueprint: Blueprint; materials: SpeakerMaterials; activity: number; haze: boolean }) { const [width, height, depth] = blueprint.body; const frontZ = depth / 2 + .012; return <><mesh geometry={blueprint.cabinet} material={materials.cabinet} /><BafflePlate width={width * .8} height={height * .74} frontZ={depth / 2 + .014} materials={materials} /><HornFlare geometry={midHorn} y={.16} frontZ={frontZ} mouthWidth={.56} mouthHeight={.2} throatWidth={.12} throatHeight={.07} depth={.19} materials={materials} /><WooferAssembly size={.22} y={-.2} frontZ={frontZ + .022} materials={materials} /><ReflexPort x={0} y={-.35} frontZ={frontZ + .025} width={.38} height={.05} /><CornerFasteners width={width * .82} height={height * .74} frontZ={frontZ + .03} inset={.055} />{haze && <LocalGlow color={MID} activity={activity} position={[0, .08, frontZ + .035]} size={.42} />}</>; }
function HighModel({ blueprint, materials, activity, haze }: { blueprint: Blueprint; materials: SpeakerMaterials; activity: number; haze: boolean }) { const [width, height, depth] = blueprint.body; const frontZ = depth / 2 + .012; return <><mesh geometry={blueprint.cabinet} material={materials.cabinet} /><BafflePlate width={width * .86} height={height * .78} frontZ={depth / 2 + .014} materials={materials} /><HornFlare geometry={highHorn} y={0} frontZ={frontZ} mouthWidth={.88} mouthHeight={.38} throatWidth={.12} throatHeight={.08} depth={.36} materials={materials} /><CornerFasteners width={width * .87} height={height * .75} frontZ={frontZ + .03} inset={.06} />{haze && <LocalGlow color={HIGH} activity={activity} position={[0, 0, frontZ + .035]} size={.58} />}</>; }

export function SpeakerMiniature({ kind, activity, selected = false, showHaze = true, idleVisible = false }: Props) { const blueprint = blueprints[kind]; const materials = useSpeakerMaterials(kind, activity); return <group>{idleVisible && <lineSegments geometry={blueprint.edges} material={idleEdgeMaterial} rotation={kind === "full" ? [-.04, 0, 0] : undefined} />}{kind === "sub" && <SubModel blueprint={blueprint} materials={materials} activity={activity} haze={showHaze} />}{kind === "woofer" && <WooferModel blueprint={blueprint} materials={materials} activity={activity} haze={showHaze} />}{kind === "full" && <FullModel blueprint={blueprint} materials={materials} activity={activity} haze={showHaze} />}{kind === "mid" && <MidModel blueprint={blueprint} materials={materials} activity={activity} haze={showHaze} />}{kind === "high" && <HighModel blueprint={blueprint} materials={materials} activity={activity} haze={showHaze} />}{selected && <mesh geometry={blueprint.cabinet} scale={[1.018, 1.018, 1.018]} rotation={kind === "full" ? [-.04, 0, 0] : undefined} material={selectionMaterial} />}</group>; }
export const speakerBlueprints = blueprints;
